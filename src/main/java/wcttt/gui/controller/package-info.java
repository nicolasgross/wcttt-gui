/**
 * <p>Contains the controller implementations.</p>
 *
 * <p>There is one controller for at least one .fxml file. Inheritance appears
 * in the following places:</p>
 *
 * <p>1. Abstract class {@link wcttt.gui.controller.Controller} is the
 * superclass of abstract class {@link wcttt.gui.controller.SubscriberController}.
 * <br>
 * 2. Every controller implements either {@link wcttt.gui.controller.Controller}
 * or {@link wcttt.gui.controller.SubscriberController}.</p>
 *
 */
package wcttt.gui.controller;
